__pycache__/
*.pyc
*.pyo
dist/
build/
*.spec
*.egg-info/
.env
_run_dashboard.py
*.xlsx
.vercel
.env*.local

# Claude Code machine-local files (never deploy these)
.claude/settings.local.json
.claude/worktrees/

# misa-fetcher machine-local state (secrets live in .env, ignored above)
node_modules/
misa-fetcher/.state/
misa-fetcher/out/
# Added by code-review-graph
.code-review-graph/

# Rendered artefacts — 100 MB+ of images, regenerate with output/collage/render-collage.js
output/

# training/ is its own git repo, synced separately
training/

# Machine-local agent state
.impeccable/hook.cache.json
.impeccable/config.local.json

# One-off data exports
dashboard/dist-export-*.csv
# Staged inputs / resumable state for scripts/vietmap-audit.mjs
dashboard/scripts/_audit-*
