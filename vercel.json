{
  "crons": [
    {
      "path": "/api/schedule-job",
      "schedule": "0 22 * * *"
    }
  ],
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_COMMIT_REF\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"master\" ]; then exit 0; else exit 1; fi"
}
